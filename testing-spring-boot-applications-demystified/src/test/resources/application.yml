logging:
  level:
    org:
      springframework:
        test:
          context: INFO # use TRACE for insights
spring:
  security:
    user:
      password: dummy
      name: user
      roles: USER
